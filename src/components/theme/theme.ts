import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#CAAE77',
    },
    secondary: {
      main: '#1E1E1E',
    },
    info: {
      main: "#F0F0F0",
    },
    background: {
      default: "#F0F0F0",
    }
  },
});

export default theme;